# Phải khớp với phiên bản MLflow Tracking Server đang chạy trong cluster
mlflow==3.1.4

# Để boto3 kết nối MinIO (S3-compatible)
boto3==1.34.0
botocore==1.34.0
kserve==0.11.2
protobuf
setuptools==69.0.3
# Thêm các thư viện mà model của bạn cần (PhoBERT)
transformers==4.41.0
torch
# numpy